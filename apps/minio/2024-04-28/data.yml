additionalProperties:
  formFields:
    - default: "minio/minio:RELEASE.2024-04-28T17-53-50Z"
      edit: true
      envKey: MINIO_IMAGE
      labelEn: Image source
      labelZh: 镜像源
      required: true
      type: select
      values:
        - label: GitHub Registry
          value: "quay.io/minio/minio:RELEASE.2024-04-28T17-53-50Z"
        - label: Docker Hub
          value: "minio/minio:RELEASE.2024-04-28T17-53-50Z"
    - default: "/home/minio"
      edit: true
      envKey: MINIO_ROOT_PATH
      labelEn: Data persistence root path
      labelZh: 数据持久化 根路径
      required: true
      type: text
    - default: "minioadmin"
      edit: true
      envKey: PANEL_MINIO_ROOT_USER
      labelEn: Root User
      labelZh: 超级用户名
      required: false
      type: text
    - default: "minioadmin"
      edit: true
      envKey: PANEL_MINIO_ROOT_PASSWORD
      labelEn: Password
      labelZh: 超级用户密码
      required: false
      type: password
    - default: 9001
      edit: true
      envKey: PANEL_APP_PORT_HTTP
      labelEn: Web UI Port
      labelZh: Web UI 端口
      required: true
      type: number
    - default: 9000
      edit: true
      envKey: PANEL_APP_PORT_API
      labelEn: API Port (external)
      labelZh: API 端口 (对外)
      required: true
      type: number
    - default: 9000
      edit: true
      envKey: MINIO_API
      labelEn: API Port (internal)
      labelZh: API 端口 (对内)
      required: true
      type: number
    - default: "on"
      edit: true
      envKey: MINIO_BROWSER
      labelEn: Enable WebUI
      labelZh: 启用 WebUI
      required: true
      type: select
      values:
        - label: 开启
          value: "on"
        - label: 关闭
          value: "off"
    - default: "on"
      edit: true
      envKey: MINIO_BROWSER_LOGIN_ANIMATION
      labelEn: WebUI login animation
      labelZh: WebUI 登录动画
      required: true
      type: select
      values:
        - label: 开启
          value: "on"
        - label: 关闭
          value: "off"
    - default: "12h"
      edit: true
      envKey: MINIO_BROWSER_SESSION_DURATION
      labelEn: Session duration
      labelZh: 会话持续时间 (s/秒 m/分钟 h/小时 d/天)
      required: true
      type: text
    - default: ""
      edit: true
      envKey: MINIO_DOMAIN
      labelEn: Host FQDN
      labelZh: 主机 FQDN
      required: false
      type: text
    - default: "http://127.0.0.1:9000"
      edit: true
      envKey: MINIO_SERVER_URL
      labelEn: API Server URL
      labelZh: API 服务器 URL (协议+主机+端口)
      required: false
      type: text
    - default: "localhost"
      edit: true
      envKey: MINIO_SERVER_HOST
      labelEn: API Server HOST
      labelZh: API 服务器 主机名 (主机域名)
      required: false
      type: text
    - default: "http://127.0.0.1:9001"
      edit: true
      envKey: MINIO_BROWSER_REDIRECT_URL
      labelEn: Console redirect URL
      labelZh: 控制台重定向 URL (协议+主机+端口)
      required: false
      type: text
    - default: "/data"
      edit: true
      envKey: MINIO_VOLUMES
      labelEn: Data volume (inside container)
      labelZh: 数据卷 (容器内部)
      required: true
      type: text
