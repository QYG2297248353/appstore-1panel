additionalProperties:
  formFields:
    - default: "minio/minio:RELEASE.2024-04-06T05-26-02Z"
      edit: true
      envKey: MINIO_IMAGE
      labelEn: Image source
      labelZh: 镜像源
      required: true
      type: select
      values:
        - label: Quay Registry
          value: "quay.io/minio/minio:RELEASE.2024-04-06T05-26-02Z"
        - label: Docker Hub
          value: "minio/minio:RELEASE.2024-04-06T05-26-02Z"
    - default: "/home/minio"
      edit: true
      envKey: MINIO_ROOT_PATH
      labelEn: Data persistence root path
      labelZh: 数据持久化 根路径
      required: true
      type: text
    - default: "minioadmin"
      edit: true
      envKey: PANEL_MINIO_ROOT_USER
      labelEn: Root User
      labelZh: 超级用户
      required: false
      type: text
    - default: "minioadmin"
      edit: true
      envKey: PANEL_MINIO_ROOT_PASSWORD
      labelEn: Password
      labelZh: 超级用户密码
      required: false
      type: password
    - default: 9001
      edit: true
      envKey: PANEL_APP_PORT_HTTP
      labelEn: Port
      labelZh: 端口
      required: true
      type: number
    - default: 9000
      edit: true
      envKey: PANEL_APP_PORT_API
      labelEn: Port
      labelZh: API 端口
      required: true
      type: number
    - default: "on"
      edit: true
      envKey: MINIO_BROWSER
      labelEn: Enable WebUI
      labelZh: 启用 WebUI
      required: true
      type: select
      values:
        - label: 开启
          value: "on"
        - label: 关闭
          value: "off"
    - default: "on"
      edit: true
      envKey: MINIO_BROWSER_LOGIN_ANIMATION
      labelEn: WebUI login animation
      labelZh: WebUI 登录动画
      required: true
      type: select
      values:
        - label: 开启
          value: "on"
        - label: 关闭
          value: "off"
    - default: "12h"
      edit: true
      envKey: MINIO_BROWSER_SESSION_DURATION
      labelEn: Session duration
      labelZh: 会话持续时间 (s/秒 m/分钟 h/小时 d/天)
      required: true
      type: text
    - default: "127.0.0.1"
      edit: true
      envKey: MINIO_DOMAIN
      labelEn: API Server Domain
      labelZh: API 服务器域名
      required: false
      type: text
    - default: "http://127.0.0.1:9000"
      edit: true
      envKey: MINIO_SERVER_URL
      labelEn: API Server URL
      labelZh: API 服务器 URL
      required: false
      type: text
    - default: "http://127.0.0.1:9001"
      edit: true
      envKey: MINIO_BROWSER_REDIRECT_URL
      labelEn: Console redirect URL
      labelZh: 控制台重定向 URL
      required: false
      type: text
